# docviewer-helper

This library is created to share helper functions to view documents in as well ngx-doc-viewer and react-documents

It now also exposes shared viewer recovery policy helpers so Angular and React can use the same decision logic for Google probe recovery and Office auto-retry behavior.
